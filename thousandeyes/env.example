# ThousandEyes Enterprise Agent - Environment Variables Example
# This file serves as a reference for local testing and development

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# ThousandEyes Account Group Token (REQUIRED)
# Obtain from: ThousandEyes Portal > Settings > Agents > Enterprise Agents
TEAGENT_ACCOUNT_TOKEN=your-account-group-token-here

# =============================================================================
# ESSENTIAL SETTINGS
# =============================================================================

# Agent hostname as it appears in ThousandEyes portal
# TEAGENT_AGENT_HOSTNAME=homeassistant-agent-01

# Network mode: ipv4, ipv6, or dual
# TEAGENT_INET=ipv4

# =============================================================================
# RESOURCE LIMITS
# =============================================================================

# Memory limit in megabytes (minimum 2048 recommended)
# TEAGENT_MEMORY_LIMIT=2048M

# CPU shares for container priority
# TEAGENT_CPU_SHARES=1024

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
# TEAGENT_LOG_LEVEL=INFO

# Log file size in megabytes before rotation
# TEAGENT_LOG_FILE_SIZE=10

# =============================================================================
# PROXY CONFIGURATION (Only if proxy is enabled)
# =============================================================================

# HTTP/HTTPS Proxy with authentication
# HTTP_PROXY=http://username:password@proxy.example.com:8080
# HTTPS_PROXY=http://username:password@proxy.example.com:8080

# Or without authentication
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# SOCKS5 Proxy example
# HTTP_PROXY=socks5://username:password@proxy.example.com:1080
# HTTPS_PROXY=socks5://username:password@proxy.example.com:1080

# Lowercase variants (some systems require both)
# http_proxy=http://proxy.example.com:8080
# https_proxy=http://proxy.example.com:8080

# Proxy bypass list (comma-separated)
# NO_PROXY=localhost,127.0.0.1,.local,.internal
# no_proxy=localhost,127.0.0.1,.local,.internal

# =============================================================================
# CUSTOM DNS CONFIGURATION (Only if custom DNS is enabled)
# =============================================================================

# Comma-separated list of DNS servers
# TEAGENT_DNS_SERVERS=8.8.8.8,8.8.4.4

# =============================================================================
# SECURITY OPTIONS
# =============================================================================

# Disable BrowserBot (set to 1 to disable)
# TEAGENT_BROWSERBOT_DISABLED=0

# Accept self-signed certificates (set to 1 to enable)
# TEAGENT_ACCEPT_SELF_SIGNED_CERTS=0

# =============================================================================
# ADVANCED OPTIONS
# =============================================================================

# Disable crash reports (set to 1 to disable)
# TEAGENT_CRASH_REPORTS_DISABLED=0

# Disable auto-update (set to 1 to disable)
# TEAGENT_AUTO_UPDATE_DISABLED=0

# =============================================================================
# VOLUME PATHS
# =============================================================================

# Custom paths for agent data and logs
# TEAGENT_LIB_PATH=/var/lib/te-agent
# TEAGENT_LOG_PATH=/var/log/te-agent

# =============================================================================
# NOTES FOR LOCAL TESTING
# =============================================================================

# To test locally with Docker:
# 1. Copy this file to .env: cp env.example .env
# 2. Edit .env and add your actual ThousandEyes account token
# 3. Uncomment and configure any additional options needed
# 4. Run: docker-compose up (if using docker-compose)
#    Or: docker run --env-file .env thousandeyes/enterprise-agent:latest

# For Home Assistant add-on:
# - These environment variables are automatically set by the run.sh script
# - Configure options through the Home Assistant UI instead
# - This file is for reference only

# =============================================================================
# DOCKER COMPOSE EXAMPLE
# =============================================================================

# version: '3.8'
# services:
#   thousandeyes-agent:
#     image: thousandeyes/enterprise-agent:latest
#     container_name: te-agent
#     env_file: .env
#     cap_add:
#       - NET_ADMIN
#       - SYS_ADMIN
#     volumes:
#       - ./data/te-agent-lib:/var/lib/te-agent
#       - ./data/te-agent-logs:/var/log/te-agent
#     restart: unless-stopped

# =============================================================================
# SECURITY WARNINGS
# =============================================================================

# WARNING: Never commit your actual .env file with real credentials to git!
# Add .env to your .gitignore file
# Keep your TEAGENT_ACCOUNT_TOKEN secure and never share it publicly

