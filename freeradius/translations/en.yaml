---
configuration:
  db_type:
    name: Database Type
    description: Database backend - SQLite (local file), MySQL (auto-discovers MariaDB add-on), or PostgreSQL
  db_host:
    name: Database Host
    description: Database server hostname (leave empty to auto-discover MariaDB add-on)
  db_port:
    name: Database Port
    description: Database server port (3306 for MySQL, 5432 for PostgreSQL)
  db_name:
    name: Database Name
    description: Database name (for SQLite, this is the filename in /config/)
  db_user:
    name: Database User
    description: Username for database connection (leave empty to auto-discover from MariaDB add-on)
  db_password:
    name: Database Password
    description: Password for database connection (leave empty to auto-discover from MariaDB add-on)
  api_auth_token:
    name: API Auth Token
    description: Bearer token for the management API (leave empty to auto-generate - check logs)
  log_level:
    name: Log Level
    description: FreeRADIUS logging verbosity (debug, info, warn, error)
  radius_auth_port:
    name: RADIUS Auth Port
    description: Port for RADIUS authentication (default 1812)
  radius_acct_port:
    name: RADIUS Accounting Port
    description: Port for RADIUS accounting (default 1813)
  radsec_port:
    name: RadSec Port
    description: Port for RadSec/RADIUS over TLS (default 2083)
  coa_port:
    name: CoA Port
    description: Port for Change of Authorization/Disconnect Messages (default 3799)
  api_port:
    name: API Port
    description: Port for the internal management API (default 8000)
